@using Haihv.Elis.Tools.Export.App.Shared.Settings
<FluentNavMenu Id="main-menu" Collapsible="true" Width="250" Title="Navigation menu" @bind-Expanded="_expanded"
               CustomToggle="true">
    <FluentNavLink Href="/" Match="NavLinkMatch.All" Icon="@(new Icons.Regular.Size20.Home())" IconColor="Color.Accent">
        Home
    </FluentNavLink>
    <FluentNavLink Href="@UriPath.ConnectionSettings" 
                   Match="NavLinkMatch.All" 
                   Icon="@(new Icons.Regular.Size20.DataUsageSettings())" 
                   IconColor="Color.Accent"
                   Tooltip="Cấu hình kết nối dữ liệu">
        Cấu hình kết nối
    </FluentNavLink>
</FluentNavMenu>

@code {
    private bool _expanded = true;
}
